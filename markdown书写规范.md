# 这是一级标题 
## 这是二级标题 
### 这是三级标题

- 无序列表项一
- 无序列表项二
  - 子列表项（缩进2个空格）
  - 子列表项 

1. 有序列表项一 
2. 有序列表项二

- [ ] 待办事项
- [x] 已完成事项

> 这是一段引用文本。 
>
>这是引用的第二段。
 
>>这是嵌套引用。

请使用 `print()` 函数来输出内容。 

```python 
def hello(): 
    print("Hello, World!")
```

[访问百度](https://www.baidu.com "百度一下")

![这是一张风景图](image.png "风景图标题")

| 左对齐 | 居中对齐 | 右对齐 |
| :----- | :------: | -----: |
| 内容A  |  内容B   |  内容C |

这是 **粗体文本**，这是 *斜体文本*，这是 ~~删除线文本~~。

数学公式：许多编辑器支持 LaTeX 语法。使用  `$ ... $`  表示行内公式.
$f(x)=ax+b$

脚注：使用 [^1] 在文中标注，并在文档末尾用 [^1]: 脚注内容 来定义

HTML 标签：尽量避免在 Markdown 文档中直接使用 HTML 标签，以保持其纯文本的简洁性和可移植性。

特殊符号转义：如果需要显示 Markdown 语法中的特殊字符（如 *, #, [），可以在其前面加上反斜杠 \ 进行转义。例如，\# 会显示为 #。

注释：可以使用 HTML 注释语法 <!-- 这是注释 -->，这部分内容在渲染后的文档中不可见。
